/*
    Container name;
    for(Pointer& ptr : data) {
        if(auto per = std::dynamic_pointer_cast<FullTimeEmployee>(ptr)) {
            name.emplace_back(ptr);
        }
    }
    return name;
*/
#include <iostream>

int main() {
    if (auto per = std::dynamic_pointer_cast</**/>()) {
        
    }
}